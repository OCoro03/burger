<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}Feasting Time!{{else}}Try again later{{/if}}
	</button>
	<button class="delete" data-id="{{this.id}}">
		DELETE 
	</button>
</li>
