<tr class="table-row">
    {{!-- {{burger_name}} --}}
    <td class="table-first-col">{{id}}. {{burger_name}}</td>
    <td>
        <button class="change-devour" data-id="{{id}}">
            Devour it!
        </button>
    </td>
</tr>